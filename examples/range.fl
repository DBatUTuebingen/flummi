CALL (§SELECT 1, 10§[]) IN
FUN (l: §int§, u: §int§) -> §int§: {
  c: §bool§;
  LOOP loop {
    EMIT l;
    l <- §{0} + 1§[l];
    c <- §{0} >= {1}§[l,u];
    IF c
    THEN STOP
    ELSE CONTINUE loop
  }
}
