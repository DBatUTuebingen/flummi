CALL range(§SELECT 1, 10§[]) IN
FUN range(l: §int§, u: §int§) -> §int§: {
  K: §bool§;
  LOOP loop {
    EMIT l;
    l <- §{0} + 1§[l];
    K <- §{0} >= {1}§[l,u];
    IF K
    THEN STOP
    ELSE CONTINUE loop
  }
}
