{
  DECLARE s : §text§;
  DECLARE i : §int§;
  DECLARE n : §int§;

  LET n = §10§;

  FORK i = §SELECT * FROM range({n})§;

  IF §random() >= 0.5§
  THEN LET s = §'tails'§
  ELSE LET s = §'heads'§;

  GATHER i = §count(*)§ BY s;
  LET i = §{i} / ({n} :: float)§;
  GATHER i = §avg({i})§;
  EMIT i
}
