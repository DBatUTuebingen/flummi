CALL (§4§[], §5§[]) IN
FUN (a: §int§, b: §int§) -> §int§: {
  c: §int§;

  c <- §0§[];

  LOOP {
    c <- §{0} + {1}§[a, c];

    EMIT §{0}§[c];

    b <- §{0} - 1§[b];

    IF §0 = {0}§[b]
      THEN BREAK
      ELSE CONTINUE
  };

  STOP
}
