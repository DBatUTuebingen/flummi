CALL add(§SELECT 4, 10§[]) IN
FUN add(a: §int§, b: §int§) -> §int§: {
  k: §bool§;

  LOOP loop {
    a, b <- §SELECT {0} + 1, {1} - 1§[a, b];


    k <- §SELECT 0 = {0}§[b];
    IF k
      THEN RETURN a
      ELSE CONTINUE loop
  }
}
