CALL (§4§[], §5§[]) IN
FUN (a: §int§, b: §int§) -> §int§: {
  LOOP loop {
    b <- §1§[];
    LOOP loop2 {
      d <- §3§[];
      BREAK loop2
    };
    BREAK loop
  };
  c <- §{0} + {1} * {2} + {1}§[a, b, d];
  EMIT §{0}§[c];
  STOP
}
